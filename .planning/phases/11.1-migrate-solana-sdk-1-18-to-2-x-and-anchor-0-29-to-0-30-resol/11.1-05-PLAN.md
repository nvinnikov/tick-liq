---
phase: 11.1-migrate-solana-sdk-1-18-to-2-x-and-anchor-0-29-to-0-30-resol
plan: 05
type: execute
wave: 5
depends_on:
  - 11.1-01
  - 11.1-02
  - 11.1-03
  - 11.1-04
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - MIGRATE-02
tags:
  - docs
  - roadmap
  - requirements
must_haves:
  truths:
    - "ROADMAP §Phase 11.1 success criterion #1 переписан, не упоминает anchor-client/anchor-lang"
    - "ROADMAP goal statement обновлён: Solana 4.x (а не 2.x) и Anchor-out-of-scope"
    - "REQUIREMENTS.md содержит MIGRATE-01..04 как закрытые с traceability к Phase 11.1"
    - "Все 4 предыдущих bisect-safe commit остаются нетронутыми"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Актуализированное Phase 11.1 описание с success criteria, соответствующими delivered scope"
      contains: "Phase 11.1"
    - path: ".planning/REQUIREMENTS.md"
      provides: "MIGRATE-01..04 зарегистрированы и отмечены как Complete"
      contains: "MIGRATE-"
  key_links:
    - from: ".planning/REQUIREMENTS.md MIGRATE-01..04"
      to: "Phase 11.1 success criteria в ROADMAP"
      via: "traceability table"
      pattern: "MIGRATE-"
    - from: ".planning/ROADMAP.md Phase 11.1"
      to: "11.1-01..04-SUMMARY.md"
      via: "list-of-plans: completed 4/4"
      pattern: "11.1"
---

<objective>
Закрыть фазу 11.1 на уровне документов: переписать ROADMAP success criterion #1 (больше не требует Anchor 0.30+, per D-02 и OQ-5), добавить раздел MIGRATE в REQUIREMENTS.md и внести 4 новых ID в таблицу traceability, обновить progress-строку фазы.

Purpose: MIGRATE-02 redefinition — пользователь убрал Anchor из scope. Документация должна соответствовать реальности, иначе будущий verifier будет bloking на несуществующей зависимости. Также плейсхолдер `Requirements: TBD` для REQUIREMENTS.md закрывается.

Output: обновлённые `.planning/ROADMAP.md` и `.planning/REQUIREMENTS.md`. Коммит `docs(11.1): update ROADMAP and REQUIREMENTS post-phase`. Фаза технически закрыта.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11.1-migrate-solana-sdk-1-18-to-2-x-and-anchor-0-29-to-0-30-resol/11.1-CONTEXT.md
@.planning/phases/11.1-migrate-solana-sdk-1-18-to-2-x-and-anchor-0-29-to-0-30-resol/11.1-RESEARCH.md

<interfaces>
Текущая ROADMAP §Phase 11.1 карточка (строки ~47-57): требует переписи success criterion #1, списка plans и progress-строки.
Текущий REQUIREMENTS.md: MIGRATE-* секция отсутствует; нужно добавить.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Переписать ROADMAP Phase 11.1 карточку, summary-list и progress-table</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (строки 27-58 — карточка Phase 11.1; строки 115-135 — progress table)
    - .planning/phases/11.1-migrate-solana-sdk-1-18-to-2-x-and-anchor-0-29-to-0-30-resol/11.1-CONTEXT.md §D-01 D-02
  </read_first>
  <action>
### 1.1 Карточка Phase 11.1 в "Phase Details"

Найти блок, начинающийся строкой `### Phase 11.1: Solana SDK 2.x Migration (INSERTED — URGENT)` (около строки 47). Заменить полностью блок от этой строки до пустой строки перед `### Phase 6: Pool Census` на:

```markdown
### Phase 11.1: Solana SDK 4.x Migration + binance-sdk v45 + edition 2024 (INSERTED — COMPLETED)
**Goal**: Upgrade Solana Rust stack from 1.18 to 4.x (single-major jump, skipping 2.x per D-01), replace raw tokio-tungstenite CEX feed with official `binance-sdk v45`, adopt Rust edition 2024 + resolver 3, and clear the 4 historical RUSTSEC advisories (curve25519-dalek, ed25519-dalek, quinn-proto, ring) that were ignored in `audit.toml` pending this migration. **Anchor (anchor-client / anchor-lang) intentionally OUT of scope** — it is not a direct dependency and Anchor 1.x would pin Solana 3.x, blocking the 4.x target (D-02).
**Depends on**: Phase 11 (the raw tungstenite feed lives in Phase 11 code).
**Requirements**: MIGRATE-01 (Solana 4.x compile), MIGRATE-02 (Anchor redefined — out of scope), MIGRATE-03 (all 4 CVEs resolved), MIGRATE-04 (binance-sdk v45 integrated).
**Success Criteria** (what must be TRUE):
  1. `Cargo.toml` pins `solana-client = "4.0.0-beta.7"` and `solana-sdk = "4"`; adopts `edition = "2024"`, `resolver = "3"`, `rust-version = "1.86"`. No Anchor dependency is added in this phase (OQ-5 resolution). `cargo build` and `cargo test` pass.
  2. `audit.toml` ignore list is empty (or contains only newly surfaced low/info advisories, each annotated with RUSTSEC-ID, transitive source, upstream-link, and reason per D-13/D-14). `cargo audit` reports 0 high/critical vulnerabilities.
  3. `src/data/cex_ws.rs` is a thin wrapper on `binance_sdk::spot::SpotWsStreams` (connect → book_ticker → on_message → disconnect). Public contract (`CexPrice`, `CexPriceState`, `watch_binance_price`) and `--cex-symbol` CLI behaviour unchanged.
  4. All existing tests (350+ unit tests) still pass; manual 5-minute live watch with `--cex-symbol SOLUSDT` produces Binance mid-price writes to `pnl_history.price` at ≥ 1 Hz, matching Binance mid ± 1% (D-18 verified).
  5. Workspace uses `resolver = "3"` and `edition = "2024"`; no regressions in `cargo clippy -- -D warnings`.
**Plans**: 5 plans
- [x] 11.1-01-PLAN.md — feat(11.1): migrate solana-sdk 1.18 → 4.x
- [x] 11.1-02-PLAN.md — feat(11.1): replace tokio-tungstenite CEX feed with binance-sdk v45
- [x] 11.1-03-PLAN.md — build(11.1): adopt edition 2024 and resolver 3
- [x] 11.1-04-PLAN.md — chore(11.1): clear audit.toml ignore list + manual smoke
- [x] 11.1-05-PLAN.md — docs(11.1): update ROADMAP and REQUIREMENTS post-phase
```

### 1.2 Строка в summary list (section "Phases", около строки 30)

Найти:
```
- [ ] **Phase 11.1: Solana SDK 2.x Migration** (URGENT — inserted) — upgrade Solana 1.18→2.x + Anchor 0.29→0.30+; closes 4 CVEs and unblocks binance-sdk v45
```

Заменить на (дата completion — placeholder, executor подставит actual merge date в формате YYYY-MM-DD):
```
- [x] **Phase 11.1: Solana SDK 4.x Migration + binance-sdk v45** — upgrade Solana 1.18→4.x, swap CEX feed to binance-sdk v45, adopt edition 2024 + resolver 3, clear 4 CVEs. Anchor out of scope (D-02). Completed {{DATE}}.
```

Executor заменяет `{{DATE}}` на actual merge date (`date +%Y-%m-%d`) в момент создания commit'а.

### 1.3 Progress table (около строки 128)

Найти:
```
| 11.1. Solana SDK 2.x Migration | v1.1 | 0/0 | Not started (URGENT) | - |
```

Заменить на (дата — тот же `{{DATE}}` placeholder, заполняется в момент commit'а):
```
| 11.1. Solana SDK 4.x Migration + binance-sdk v45 | v1.1 | 5/5 | Complete | {{DATE}} |
```

### Запреты
- НЕ менять другие фазы (11, 6-10, v1.0 section).
- НЕ трогать v1.0 archive.
- НЕ трогать src/**, Cargo.toml, audit.toml в этом таске.
  </action>
  <verify>
    <automated>grep -c 'Phase 11.1: Solana SDK 4.x Migration' .planning/ROADMAP.md | awk '$1 >= 2 {exit 0} {exit 1}' && ! grep -q 'anchor-client' .planning/ROADMAP.md && grep -q '11.1-05-PLAN.md' .planning/ROADMAP.md && ! grep -q 'Phase 11.1: Solana SDK 2.x Migration' .planning/ROADMAP.md && grep -qE '5/5 \| Complete \| [0-9]{4}-[0-9]{2}-[0-9]{2}' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Phase 11.1: Solana SDK 4.x Migration' .planning/ROADMAP.md` >= 2 (summary + Phase Details + progress line)
    - `grep -c 'Phase 11.1: Solana SDK 2.x Migration' .planning/ROADMAP.md` = 0
    - `grep -c 'anchor-client' .planning/ROADMAP.md` = 0
    - `grep -c 'anchor-lang' .planning/ROADMAP.md` = 0
    - `grep -c '11.1-05-PLAN.md' .planning/ROADMAP.md` = 1
    - `grep -c 'Anchor.*out of scope\|out of scope.*Anchor\|Anchor scope redefined\|OUT of scope' .planning/ROADMAP.md` >= 1
    - `grep -c 'solana-sdk = "4"' .planning/ROADMAP.md` >= 1
    - `grep -c 'binance-sdk v45' .planning/ROADMAP.md` >= 1
    - `grep -qE '5/5 \| Complete \| [0-9]{4}-[0-9]{2}-[0-9]{2}' .planning/ROADMAP.md` (completion date — actual merge date в формате YYYY-MM-DD; НЕ placeholder `{{DATE}}`)
    - `grep -c '{{DATE}}' .planning/ROADMAP.md` = 0 (все placeholder'ы подставлены на actual date)
    - Прочие фазы (Phase 11, Phase 6, Phase 7, Phase 8, Phase 9, Phase 10) — diff = 0 строк (не тронуты)
  </acceptance_criteria>
  <done>ROADMAP.md отражает delivered scope: Solana 4.x, Anchor out of scope, 5/5 plans complete, completion date = actual merge date.</done>
</task>

<task type="auto">
  <name>Task 2: Добавить MIGRATE-01..04 в REQUIREMENTS.md + traceability rows</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (весь — 105 строк; понять структуру секций и taxonomy table)
    - .planning/phases/11.1-migrate-solana-sdk-1-18-to-2-x-and-anchor-0-29-to-0-30-resol/11.1-CONTEXT.md §D-01..D-19
  </read_first>
  <action>
### 2.1 Добавить новую секцию MIGRATE после "Strategy Specification (SPEC)"

Найти блок (строки ~49-53):
```
### Strategy Specification (SPEC)

- [ ] **SPEC-01**: ...
- [ ] **SPEC-02**: ...
- [ ] **SPEC-03**: ...
```

Сразу ПОСЛЕ `- [ ] **SPEC-03**: ...` строки, ДО `---` разделителя, добавить новый блок:

```markdown

### Infrastructure Migration (MIGRATE) — Phase 11.1

> **Scope redefinition:** Originally paired Solana + Anchor bumps; user decided (D-02) to take Solana 1.18 → 4.x as a single jump and treat Anchor as out-of-scope since it is not a direct dependency. Anchor will be added separately when Anchor 2.x ships under Solana 4.x and/or LIVE-02 unblocks.

- [x] **MIGRATE-01**: Solana Rust stack (`solana-client`, `solana-sdk`, transitive deps) compiles on 4.x with edition 2024 + resolver 3; all 350+ unit tests pass.
- [x] **MIGRATE-02**: Anchor scope redefined — no `anchor-client` / `anchor-lang` dependency is added in this phase. Deferred to post-v1.1 work blocked by LIVE-02.
- [x] **MIGRATE-03**: The four historical RUSTSEC advisories (RUSTSEC-2024-0344, RUSTSEC-2022-0093, RUSTSEC-2026-0037, RUSTSEC-2025-0009) are removed from `audit.toml`; `cargo audit` exits 0 with no high/critical vulnerabilities.
- [x] **MIGRATE-04**: `src/data/cex_ws.rs` is rewritten as a thin wrapper over `binance-sdk v45` (SpotWsStreams + BookTicker); public contract and `--cex-symbol` CLI behaviour unchanged; 5-minute live smoke confirmed Binance mid writes at ≥ 1 Hz.

```

### 2.2 Обновить Traceability table (в конце файла)

Найти строку `Coverage: 21/21 v1.1 requirements mapped to phases 6–10. ✓` (около строки 80).

Заменить на:
```
Coverage: 25/25 v1.1 requirements mapped to phases 6–11.1. ✓ (21 research + 4 infrastructure)
```

Найти финальную таблицу `| REQ-ID | Phase | Status |` и после последней строки `| SPEC-03 ... | Pending |` добавить 4 новых строки:

```
| MIGRATE-01 | Phase 11.1 — Solana SDK Migration         | Complete |
| MIGRATE-02 | Phase 11.1 — Solana SDK Migration         | Complete (scope redefined per D-02) |
| MIGRATE-03 | Phase 11.1 — Solana SDK Migration         | Complete |
| MIGRATE-04 | Phase 11.1 — Solana SDK Migration         | Complete |
```

### Запреты
- НЕ менять существующие CENSUS/FILTER/DEEP/LAND/SIZE/SPEC записи.
- НЕ менять "Future Requirements (Deferred beyond v1.1)" секцию.
- НЕ менять "Out of Scope (v1.1)" секцию.
- Формат табличных строк — ровно 3 колонки `| REQ-ID | Phase | Status |`, с выравниванием как у существующих записей.
  </action>
  <verify>
    <automated>grep -c '\*\*MIGRATE-01\*\*' .planning/REQUIREMENTS.md | grep -qE '^1$' && grep -c '\*\*MIGRATE-02\*\*' .planning/REQUIREMENTS.md | grep -qE '^1$' && grep -c '\*\*MIGRATE-03\*\*' .planning/REQUIREMENTS.md | grep -qE '^1$' && grep -c '\*\*MIGRATE-04\*\*' .planning/REQUIREMENTS.md | grep -qE '^1$' && grep -q 'Coverage: 25/25' .planning/REQUIREMENTS.md && grep -c '| MIGRATE-01 | Phase 11.1' .planning/REQUIREMENTS.md | grep -qE '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\*\*MIGRATE-01\*\*' .planning/REQUIREMENTS.md` = 1
    - `grep -c '\*\*MIGRATE-02\*\*' .planning/REQUIREMENTS.md` = 1
    - `grep -c '\*\*MIGRATE-03\*\*' .planning/REQUIREMENTS.md` = 1
    - `grep -c '\*\*MIGRATE-04\*\*' .planning/REQUIREMENTS.md` = 1
    - `grep -c '\- \[x\] \*\*MIGRATE-' .planning/REQUIREMENTS.md` = 4 (все 4 отмечены как выполненные)
    - `grep -c 'Coverage: 25/25' .planning/REQUIREMENTS.md` = 1
    - `grep -c '| MIGRATE-' .planning/REQUIREMENTS.md` = 4 (строки в таблице traceability)
    - `grep -c 'Infrastructure Migration (MIGRATE) — Phase 11.1' .planning/REQUIREMENTS.md` = 1
    - Существующие записи CENSUS-01, FILTER-01, DEEP-01, LAND-01, SIZE-01, SPEC-01 — неизменны (все 21 ID всё ещё присутствуют)
    - `grep -c 'CENSUS-01\|FILTER-01\|DEEP-01\|LAND-01\|SIZE-01\|SPEC-01' .planning/REQUIREMENTS.md` >= 12 (каждый ID в секции + в таблице traceability)
  </acceptance_criteria>
  <done>REQUIREMENTS.md документирует MIGRATE-01..04 как завершённые, с traceability к Phase 11.1.</done>
</task>

<task type="auto">
  <name>Task 3: Atomic commit обновлений документов + regression check предыдущих 4 commit'ов</name>
  <files>(git operation only)</files>
  <read_first>
    - `git status` — убедиться, что staged changes — только `.planning/ROADMAP.md` и `.planning/REQUIREMENTS.md`
    - .planning/phases/11.1-migrate-solana-sdk-1-18-to-2-x-and-anchor-0-29-to-0-30-resol/11.1-CONTEXT.md §D-11 D-12
  </read_first>
  <action>
Sanity:
```bash
git status --short
git diff --stat HEAD
```

Ожидание: diff затрагивает только `.planning/ROADMAP.md` и `.planning/REQUIREMENTS.md`. Никаких src/**, Cargo.*, audit.toml — если есть, остановиться и поднять CHECKPOINT.

Commit:
```bash
git add .planning/ROADMAP.md .planning/REQUIREMENTS.md
git commit -m "docs(11.1): update ROADMAP and REQUIREMENTS post-phase

- ROADMAP §Phase 11.1: goal + success criteria rewritten to reflect Solana 4.x
  (not 2.x) and Anchor out of scope (D-02 / OQ-5).
- ROADMAP progress table: phase 11.1 marked 5/5 Complete (actual merge date).
- REQUIREMENTS.md: added Infrastructure Migration (MIGRATE) section with
  MIGRATE-01..04 all marked [x]. Traceability table updated (21 -> 25
  requirements, + 4 infrastructure IDs mapped to Phase 11.1).
- MIGRATE-02 scope note: Anchor deferred until Anchor 2.x ships under
  Solana 4.x and/or LIVE-02 unblocks.

Docs-only; no code touched. Bisect chain from Plans 01-04 intact."
```

НЕ использовать `--amend`. НЕ использовать `git add -A` / `git add .` (риск захватить случайные .planning/phases SUMMARY, которые появятся на этапе execute).

### Regression check: 5 commits в истории ветки с ожидаемыми заголовками

Content-based check (НЕ строгий positional `awk NR==N` — Plan 05 depends_on [01, 02, 03, 04] уже гарантирует порядок через frontmatter; дополнительные intermediate commits, если они когда-либо появятся, не должны валить regression):

```bash
# Все 5 ожидаемых commit headline'ов должны присутствовать в истории ветки от merge-base с master.
BASE=$(git merge-base HEAD master)
LOG=$(git log --oneline "$BASE"..HEAD)
echo "$LOG" | grep -q 'feat(11.1): migrate solana-sdk' || { echo "MISSING: Plan 01 commit"; exit 1; }
echo "$LOG" | grep -q 'feat(11.1): replace tokio-tungstenite CEX feed' || { echo "MISSING: Plan 02 commit"; exit 1; }
echo "$LOG" | grep -q 'build(11.1): adopt edition 2024' || { echo "MISSING: Plan 03 commit"; exit 1; }
echo "$LOG" | grep -q 'chore(11.1): clear audit.toml ignore list' || { echo "MISSING: Plan 04 commit"; exit 1; }
echo "$LOG" | grep -q 'docs(11.1): update ROADMAP and REQUIREMENTS' || { echo "MISSING: Plan 05 commit"; exit 1; }
echo "All 5 phase commits present in branch."
```

Если какой-то commit отсутствует — это BLOCKER, НЕ мержить; разобраться, какой Plan не произвёл commit. Если обнаружился посторонний commit (например `chore(11.1): cargo fix prep` от Plan 03) — это НАРУШЕНИЕ D-11 атомарности Plan 03; вернуться в Plan 03 и исправить (rebase/squash тот sub-commit в основной `build(11.1): adopt edition 2024 ...`).
  </action>
  <verify>
    <automated>git log -1 --format='%s' | grep -qE '^docs\(11\.1\): update ROADMAP and REQUIREMENTS' && git diff HEAD^ HEAD --name-only | sort | tr '\n' ',' | grep -qE '^\.planning/REQUIREMENTS\.md,\.planning/ROADMAP\.md,$'</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format='%s'` matches `/^docs\(11\.1\): update ROADMAP and REQUIREMENTS/`
    - `git diff HEAD^ HEAD --name-only` возвращает ТОЛЬКО две строки: `.planning/ROADMAP.md` и `.planning/REQUIREMENTS.md`
    - `git diff HEAD^ HEAD -- src/` пусто
    - `git diff HEAD^ HEAD -- Cargo.toml` пусто
    - `git diff HEAD^ HEAD -- audit.toml` пусто
    - **Content-based regression check (замена строгого `awk NR==N` positional grep):** Каждый из 5 ожидаемых headline'ов присутствует в `git log --oneline $(git merge-base HEAD master)..HEAD`:
      - `git log --oneline $(git merge-base HEAD master)..HEAD | grep -q 'feat(11.1): migrate solana-sdk'` = true
      - `git log --oneline $(git merge-base HEAD master)..HEAD | grep -q 'feat(11.1): replace tokio-tungstenite CEX feed'` = true
      - `git log --oneline $(git merge-base HEAD master)..HEAD | grep -q 'build(11.1): adopt edition 2024'` = true
      - `git log --oneline $(git merge-base HEAD master)..HEAD | grep -q 'chore(11.1): clear audit.toml ignore list'` = true
      - `git log --oneline $(git merge-base HEAD master)..HEAD | grep -q 'docs(11.1): update ROADMAP and REQUIREMENTS'` = true
    - Порядок commit'ов подразумевается через `depends_on: [01, 02, 03, 04]` в frontmatter этого plan'а; контiguous positional ordering (`awk NR==1` == docs, `NR==2` == chore, ...) НЕ требуется.
    - **D-11 атомарность Plan 03:** `git log --oneline $(git merge-base HEAD master)..HEAD | grep -cE '^[a-f0-9]+ chore\(11\.1\): cargo fix prep' ` = 0 (такого commit'а быть не должно — Plan 03 теперь запрещает отдельный sub-commit).
  </acceptance_criteria>
  <done>Phase 11.1 закрыта полностью. ROADMAP и REQUIREMENTS актуальны. 5 атомарных commits в ветке; каждый проверен по headline'у (content-based, не positional).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .planning/ROADMAP.md / REQUIREMENTS.md | Single source of truth for phase completion — tampering here mis-represents what shipped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.1-17 | Repudiation | Misleading success criteria in ROADMAP | mitigate | Task 1 explicitly rewrites criterion #1 to match delivered scope. Task 2 cross-links MIGRATE-* back to Phase 11.1. Audit trail preserved via commit message. |
| T-11.1-18 | Tampering | Accidental deletion of other phase content in large ROADMAP edit | mitigate | Acceptance criteria enumerate untouched phases (11, 6-10, v1.0) — grep/diff asserts absence of modifications. |
</threat_model>

<verification>
## Exit gate

```bash
# Docs changes don't require cargo, but sanity-check the binary still builds:
cargo build --all-targets
cargo test --all
cargo clippy --all-targets -- -D warnings
cargo audit
```

Все exit 0. (Эти команды НЕ должны что-то менять — они проверяют, что бинарь по-прежнему зелёный после docs-коммита.)

## Sanity greps (post-commit)

```bash
grep -c 'Phase 11.1: Solana SDK 2.x Migration' .planning/ROADMAP.md  # 0
grep -c 'Phase 11.1: Solana SDK 4.x Migration' .planning/ROADMAP.md  # ≥ 2
grep -c 'anchor-client' .planning/ROADMAP.md                          # 0
grep -c 'MIGRATE-01\|MIGRATE-02\|MIGRATE-03\|MIGRATE-04' .planning/REQUIREMENTS.md  # ≥ 8 (секция + таблица)
grep -c 'Coverage: 25/25' .planning/REQUIREMENTS.md                   # 1
grep -c '{{DATE}}' .planning/ROADMAP.md                               # 0 (placeholder должен быть заменён)
```

## Content-based regression check (замена строгого NR==N positional grep)

```bash
BASE=$(git merge-base HEAD master)
for needle in \
  'feat(11.1): migrate solana-sdk' \
  'feat(11.1): replace tokio-tungstenite CEX feed' \
  'build(11.1): adopt edition 2024' \
  'chore(11.1): clear audit.toml ignore list' \
  'docs(11.1): update ROADMAP and REQUIREMENTS'; do
    git log --oneline "$BASE"..HEAD | grep -q "$needle" \
      && echo "OK: $needle" \
      || { echo "MISSING: $needle"; exit 1; }
done

# Anti-check: запрещённый sub-commit из Plan 03 не должен всплыть
git log --oneline "$BASE"..HEAD | grep -E 'chore\(11\.1\): cargo fix prep' \
  && { echo "FORBIDDEN commit present (D-11 violation)"; exit 1; } \
  || echo "OK: no chore(11.1): cargo fix prep"
```
</verification>

<success_criteria>
- [ ] ROADMAP §Phase 11.1 goal + success criteria больше не упоминают `anchor-client`/`anchor-lang`/0.30+.
- [ ] ROADMAP §Phase 11.1 success criterion #1 перечисляет конкретные пины (`solana-client = "4.0.0-beta.7"`, `solana-sdk = "4"`, `edition = "2024"`, `resolver = "3"`, `rust-version = "1.86"`).
- [ ] ROADMAP progress table: строка 11.1 помечена `5/5 | Complete | {actual-merge-date-YYYY-MM-DD}`; никаких `{{DATE}}` placeholder'ов в файле не остаётся.
- [ ] REQUIREMENTS.md содержит секцию "Infrastructure Migration (MIGRATE)" с 4 `- [x]` записями.
- [ ] REQUIREMENTS.md traceability table включает 4 новых строки MIGRATE-01..04 → Phase 11.1.
- [ ] `Coverage: 25/25` в REQUIREMENTS.md.
- [ ] Atomic commit `docs(11.1): update ROADMAP and REQUIREMENTS post-phase` создан; diff содержит только `.planning/ROADMAP.md` и `.planning/REQUIREMENTS.md`.
- [ ] **Regression chain (content-based):** все 5 headline'ов присутствуют в истории ветки; запрещённый `chore(11.1): cargo fix prep` отсутствует.
- [ ] `cargo build && cargo test && cargo clippy -- -D warnings && cargo audit` на HEAD — exit 0.
</success_criteria>

<output>
After completion, create `.planning/phases/11.1-migrate-solana-sdk-1-18-to-2-x-and-anchor-0-29-to-0-30-resol/11.1-05-SUMMARY.md`.
</output>
