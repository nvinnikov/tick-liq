---
phase: 11.1-migrate-solana-sdk-1-18-to-2-x-and-anchor-0-29-to-0-30-resol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - src/main.rs
  - src/rpc.rs
  - src/strategy/risk_monitor.rs
  - src/protocols/orca.rs
  - src/protocols/raydium.rs
autonomous: true
requirements:
  - MIGRATE-01
tags:
  - solana
  - migration
  - rust-sdk
must_haves:
  truths:
    - "Проект собирается на solana-sdk 4.x без использования 1.18"
    - "Все 350+ unit-тестов проходят после апгрейда SDK"
    - "cargo clippy -- -D warnings остаётся зелёным"
    - "5 исходных файлов, использующих solana_*, компилируются с путями, корректными для 4.x"
    - "Коммит бисект-совместим: на этом SHA одинарная команда `cargo build && cargo test && cargo clippy -- -D warnings` возвращает 0"
  artifacts:
    - path: "Cargo.toml"
      provides: "Pinned solana-sdk = \"4\" + solana-client = \"4.0.0-beta.7\""
      contains: "solana-sdk"
    - path: "Cargo.lock"
      provides: "Resolved solana-* 4.x lockfile"
      contains: "solana-sdk"
    - path: "src/rpc.rs"
      provides: "RpcClient import path compatible with solana-client 4.0.0-beta.7"
      contains: "RpcClient"
    - path: "src/strategy/risk_monitor.rs"
      provides: "pubkey! macro call compiled against 4.x macro path"
      contains: "pubkey!"
  key_links:
    - from: "Cargo.toml"
      to: "Cargo.lock"
      via: "cargo update -p solana-sdk -p solana-client"
      pattern: "solana-sdk"
    - from: "src/main.rs"
      to: "solana_sdk::pubkey::Pubkey"
      via: "use-statement re-export"
      pattern: "use solana_sdk::pubkey::Pubkey"
    - from: "src/rpc.rs"
      to: "solana_client::rpc_client::RpcClient"
      via: "use-statement; fallback `solana_rpc_client::rpc_client::RpcClient` if compiler requires"
      pattern: "RpcClient"
---

<objective>
Поднять Solana-стек с 1.18 на 4.x. Исправить 5 файлов, использующих `solana_*`-пути, чтобы проект собирался, тестировался и проходил clippy в бисект-совместимом виде одним коммитом.

Purpose: MIGRATE-01 — единичный атомарный шаг, который делает всё остальное (binance-sdk v45, edition 2024, чистка audit.toml) возможным. Без этого коммита `cargo audit` продолжает выдавать 4 RUSTSEC и `binance-sdk` блокируется по MSRV/resolver.

Output: Bump Cargo.toml/Cargo.lock + минимальные правки в 5 src-файлах, чтобы компилятор был счастлив. Коммит `feat(11.1): migrate solana-sdk 1.18 → 4.x`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1-migrate-solana-sdk-1-18-to-2-x-and-anchor-0-29-to-0-30-resol/11.1-CONTEXT.md
@.planning/phases/11.1-migrate-solana-sdk-1-18-to-2-x-and-anchor-0-29-to-0-30-resol/11.1-RESEARCH.md
@Cargo.toml
@audit.toml
@src/main.rs
@src/rpc.rs
@src/strategy/risk_monitor.rs
@src/protocols/orca.rs
@src/protocols/raydium.rs

<interfaces>
<!-- Контракты, которые executor увидит в репозитории. Экстракт из grep `use solana_*`. -->

Текущие solana-импорты (baseline 1.18, НЕ трогать структуру, только пути):

src/main.rs:3
```rust
use solana_sdk::pubkey::Pubkey;
```

src/rpc.rs:2-3
```rust
use solana_client::rpc_client::RpcClient;
use solana_sdk::pubkey::Pubkey;
```

src/protocols/orca.rs:4
```rust
use solana_sdk::pubkey::Pubkey;
```

src/protocols/raydium.rs:3
```rust
use solana_sdk::pubkey::Pubkey;
```

src/strategy/risk_monitor.rs:235-243 (Drift PDA derivation)
```rust
pub fn derive_drift_user_pda(
    authority: &solana_sdk::pubkey::Pubkey,
) -> solana_sdk::pubkey::Pubkey {
    let drift_program_id = solana_sdk::pubkey!("dRiftyHA39MWEi3m9aunc5MzRF1JYuBsbn6VPcn33UH");
    let (pda, _) = solana_sdk::pubkey::Pubkey::find_program_address(
        &[b"user", authority.as_ref(), &0u16.to_le_bytes()],
        &drift_program_id,
    );
    pda
}
```
+ test-only `use solana_sdk::pubkey::Pubkey;` на строках 713 и 747.

Ожидаемый результат после bump'а (RESEARCH row-by-row):
- Все `use solana_sdk::pubkey::Pubkey` остаются как есть (re-export в façade-крейте 4.x, H confidence).
- `solana_sdk::pubkey!` макрос, если компилятор ругнётся "cannot find macro", заменить на `solana_pubkey::pubkey!` (M confidence).
- `RpcClient`: при переходе на `solana-client = "4.0.0-beta.7"` (пер D-01 + user-lock) путь, скорее всего, остался `solana_client::rpc_client::RpcClient`; fallback — `solana_rpc_client::rpc_client::RpcClient`.
- Если borsh 0.10 упадёт в `src/protocols/{orca,raydium}.rs` — поднять на `borsh = "1"` внутри этого же коммита (D-11 бисект-требование).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump solana-sdk/solana-client в Cargo.toml и пересчитать Cargo.lock</name>
  <files>Cargo.toml, Cargo.lock</files>
  <read_first>
    - Cargo.toml (текущие пины `solana-client = "1.18"`, `solana-sdk = "1.18"` на строках 16-17)
    - .planning/phases/11.1-migrate-solana-sdk-1-18-to-2-x-and-anchor-0-29-to-0-30-resol/11.1-RESEARCH.md §"Cargo.toml Diff Preview"
    - .planning/phases/11.1-migrate-solana-sdk-1-18-to-2-x-and-anchor-0-29-to-0-30-resol/11.1-CONTEXT.md §Decisions D-01 + user_decisions_from_research_review
  </read_first>
  <action>
В `Cargo.toml` заменить строки:
```toml
solana-client = "1.18"
solana-sdk = "1.18"
```
на:
```toml
# Phase 11.1 (D-01 + user-locked OQ-1 resolution):
# solana-sdk — façade crate на component crates (solana-pubkey, solana-program, solana-account ...)
# solana-client 4.x всё ещё beta на 2026-04-17; пользователь принял beta-risk ради единого major surface.
solana-client = "4.0.0-beta.7"
solana-sdk = "4"
```

НЕ менять `edition`, `resolver`, `rust-version` — это задача Plan 03.
НЕ добавлять `binance-sdk` — это задача Plan 02.

Затем выполнить:
```bash
cargo update -p solana-sdk -p solana-client
```
Это регенерирует `Cargo.lock`. Если cargo выводит ошибку "failed to select a version for solana-client matching 4.0.0-beta.7" — это BLOCKER, поднять CHECKPOINT с полным текстом ошибки; НЕ откатываться на 3.1.x без разрешения пользователя (per OQ-1 user override).

Если `cargo update` жалуется на `borsh = "0.10"`, оставить как есть — компилятор отрегируется в Task 2.
  </action>
  <verify>
    <automated>grep -E '^solana-client = "4\.0\.0-beta\.7"$' Cargo.toml && grep -E '^solana-sdk = "4"$' Cargo.toml && test -f Cargo.lock && grep -q 'name = "solana-sdk"' Cargo.lock</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'solana-sdk = "1.18"' Cargo.toml` = 0
    - `grep -c 'solana-client = "1.18"' Cargo.toml` = 0
    - `grep -c 'solana-sdk = "4"' Cargo.toml` >= 1
    - `grep -c 'solana-client = "4.0.0-beta.7"' Cargo.toml` >= 1
    - `Cargo.lock` содержит секцию `name = "solana-sdk"` с версией, начинающейся на `4.`
  </acceptance_criteria>
  <done>Cargo.toml пинит solana-sdk="4" и solana-client="4.0.0-beta.7", Cargo.lock пересобран. `cargo build` может упасть — это ожидаемо; починка в Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Починить 5 файлов под 4.x API и вернуть всё в зелёный</name>
  <files>src/main.rs, src/rpc.rs, src/strategy/risk_monitor.rs, src/protocols/orca.rs, src/protocols/raydium.rs, Cargo.toml (если компилятор требует borsh 1)</files>
  <read_first>
    - src/main.rs (только строки с `use solana_*` и обёртка для Pubkey)
    - src/rpc.rs (весь — маленький файл, ~140 строк)
    - src/strategy/risk_monitor.rs строки 230-260 (derive_drift_user_pda), 280-300 (ad-hoc RpcClient::new_with_timeout), 700-750 (test-only use)
    - src/protocols/orca.rs (импорты + первые ~120 строк)
    - src/protocols/raydium.rs (импорты + тест `raydium_pool_roundtrip`)
    - .planning/phases/11.1-migrate-solana-sdk-1-18-to-2-x-and-anchor-0-29-to-0-30-resol/11.1-RESEARCH.md §"Breaking Changes per File" и §"Expected failure modes"
  </read_first>
  <behavior>
    - После всех правок `cargo build` возвращает 0.
    - После всех правок `cargo test --all` пропускает 350+ unit-тестов (ни один не сломан миграцией).
    - `cargo clippy -- -D warnings` возвращает 0.
    - Все 4 RUSTSEC, ранее перечисленные в `audit.toml`, больше не присутствуют в `cargo tree -i curve25519-dalek`/`cargo tree -i ed25519-dalek` для старых major-версий (baseline check, финальное снятие ignore'ов в Plan 04).
    - Public surface модулей `rpc`, `protocols::orca`, `protocols::raydium`, `strategy::risk_monitor`, `main` не меняется (те же функции, те же сигнатуры, только пути импорта могут быть обновлены).
  </behavior>
  <action>
Этот таск — реактивный. Порядок действий:

1. Запустить `cargo build 2>&1 | tee /tmp/build_err.log`.
2. Итеративно по каждой ошибке применять фиксы из RESEARCH.md §"Expected failure modes":

   **Если "cannot find macro `pubkey` in crate `solana_sdk`"** (ожидаемо в `src/strategy/risk_monitor.rs:238`):
   - Заменить `solana_sdk::pubkey!("dRiftyHA39MWEi3m9aunc5MzRF1JYuBsbn6VPcn33UH")` на `solana_pubkey::pubkey!("dRiftyHA39MWEi3m9aunc5MzRF1JYuBsbn6VPcn33UH")`.
   - Убедиться что `solana-pubkey` тянется транзитивно через `solana-sdk` — если нет, `cargo add solana-pubkey` (используя версию, совпадающую с той, что в `Cargo.lock` под `solana-sdk`'s deps).

   **Если "cannot find type `Pubkey` in crate `solana_sdk::pubkey`"** (все 5 файлов):
   - Попробовать сначала `use solana_sdk::pubkey::Pubkey;` (re-export ожидается, H confidence).
   - Если не работает — заменить на `use solana_pubkey::Pubkey as Pubkey;` единообразно во всех 5 файлах. Также обновить все type signatures с `solana_sdk::pubkey::Pubkey` на `solana_pubkey::Pubkey` (grep + sed).

   **Если "method `new_with_timeout` not found for `RpcClient`" ИЛИ "module `rpc_client` not found"** (`src/rpc.rs:2`, `src/strategy/risk_monitor.rs:284`):
   - Заменить `use solana_client::rpc_client::RpcClient;` на `use solana_rpc_client::rpc_client::RpcClient;` (component crate из split-SDK).
   - Если `solana-rpc-client` не в транзитивах — `cargo add solana-rpc-client` с версией, совместимой с `solana-client = "4.0.0-beta.7"`.

   **Если "trait `BorshDeserialize` not implemented for `Pubkey`"** (`src/protocols/orca.rs`, `src/protocols/raydium.rs`):
   - Поднять `borsh = "0.10"` на `borsh = "1"` в `Cargo.toml`.
   - В `src/protocols/*.rs` проверить, что `#[derive(BorshDeserialize, BorshSerialize)]` всё ещё компилируется; если потребуется — в derive-макросах добавить `#[borsh(crate = "borsh")]`.
   - Прогнать тесты `raydium_pool_roundtrip`, `raydium_position_roundtrip` (в `src/protocols/raydium.rs`) и `orca_whirlpool_roundtrip` (если есть).

   **Если `RpcClient::new_with_timeout` теперь требует `CommitmentConfig`**:
   - Передать `CommitmentConfig::confirmed()` из `solana_commitment_config::CommitmentConfig` (или `solana_sdk::commitment_config::CommitmentConfig` если re-export жив). Сохранить текущее поведение — никаких новых configuration args.

3. После каждого фикса — `cargo build`. Когда `cargo build` зелёный:
   - `cargo test` (должно пройти все 350+ unit-тестов; если какой-то упал по причине, НЕ связанной с миграцией путей — это BLOCKER, поднять CHECKPOINT).
   - `cargo clippy -- -D warnings`. Любые новые clippy-lints починить inline (переименования, `use` чистка). НЕ менять бизнес-логику.

4. ЗАПРЕТЫ:
   - НЕ трогать `src/data/cex_ws.rs` (задача Plan 02).
   - НЕ трогать `src/data/ws.rs` (остаётся на tokio-tungstenite, per D-10).
   - НЕ переключать edition/resolver (Plan 03).
   - НЕ чистить audit.toml (Plan 04).
   - НЕ редактировать ROADMAP.md (Plan 05).

5. Если возникнет необходимость в изменении cargo features для `solana-client` (например, отключить `default` фичи) — остановиться, поднять CHECKPOINT. Текущая фича-конфигурация `solana-client = "4.0.0-beta.7"` без `features = [...]` использует `default`, должно быть достаточно.
  </action>
  <verify>
    <automated>cargo build --all-targets && cargo test --all && cargo clippy --all-targets -- -D warnings && echo CLIPPY_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --all-targets` exits 0
    - `cargo test --all` exits 0 (count of "test result: ok" lines ≥ 1, no "FAILED" lines)
    - `cargo clippy --all-targets -- -D warnings` exits 0
    - `grep -rE 'solana_sdk::|solana_pubkey::|solana_client::|solana_rpc_client::' src/ | wc -l` ≥ 5 (миграция сохранила импорты)
    - `grep -c 'solana-sdk = "1.18"' Cargo.toml` = 0 (из Task 1, sanity)
    - Только исходные 5 файлов + возможно Cargo.toml/Cargo.lock модифицированы (`git diff --stat HEAD -- ':!.planning'` показывает ≤ 7 файлов)
    - НЕ модифицированы: `src/data/cex_ws.rs`, `src/data/ws.rs`, `audit.toml`, `.planning/**`, любые файлы вне `src/`/`Cargo.toml`/`Cargo.lock`
  </acceptance_criteria>
  <done>Проект собирается, все тесты зелёные, clippy чист. 5 исходных файлов используют 4.x-совместимые пути. Бисект-коммит готов.</done>
</task>

<task type="auto">
  <name>Task 3: Финальная bisect-верификация и атомарный commit</name>
  <files>(git operation only, no file edits)</files>
  <read_first>
    - `git status` — убедиться что staged changes ограничены Task 1/Task 2 артефактами
    - .planning/phases/11.1-migrate-solana-sdk-1-18-to-2-x-and-anchor-0-29-to-0-30-resol/11.1-CONTEXT.md §D-11 D-12
  </read_first>
  <action>
Выполнить финальную bisect-верификацию ПОСЛЕДОВАТЕЛЬНО:
```bash
cargo build --all-targets
cargo test --all
cargo clippy --all-targets -- -D warnings
```
Все три команды должны вернуть exit=0. Если любая упала — вернуться к Task 2, починить, повторить. НЕ продолжать с красной верификацией.

Если всё зелёное — поставить атомарный commit:
```bash
git add Cargo.toml Cargo.lock src/main.rs src/rpc.rs src/strategy/risk_monitor.rs src/protocols/orca.rs src/protocols/raydium.rs
git commit -m "feat(11.1): migrate solana-sdk 1.18 → 4.x

- Bump solana-sdk to 4, solana-client to 4.0.0-beta.7 (user-accepted beta risk per OQ-1)
- Update Pubkey / pubkey! / RpcClient imports in 5 files
- borsh bumped to 1 if compiler required (reactive per OQ-2)
- All 350+ unit tests green; cargo clippy -- -D warnings clean
- audit.toml NOT touched — cleanup scheduled in Plan 04

Bisect-safe: cargo build && cargo test && cargo clippy green on this SHA (D-12)."
```

Если были изменения в `borsh` — включить это в body commit message. Не использовать `--amend`, не использовать `--no-verify`.
  </action>
  <verify>
    <automated>git log -1 --format='%s' | grep -qE '^feat\(11\.1\): migrate solana-sdk' && cargo build --all-targets && cargo test --all && echo BUILD_TEST_OK</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --format='%s'` matches `/^feat\(11\.1\): migrate solana-sdk/`
    - `git diff HEAD^ HEAD --name-only | sort` contains exactly: `Cargo.lock`, `Cargo.toml`, `src/main.rs`, `src/protocols/orca.rs`, `src/protocols/raydium.rs`, `src/rpc.rs`, `src/strategy/risk_monitor.rs` (± Cargo.toml borsh bump)
    - Отсутствуют в diff: `.planning/**`, `src/data/**`, `audit.toml`
    - `cargo build --all-targets && cargo test --all && cargo clippy --all-targets -- -D warnings` возвращает 0 на HEAD
  </acceptance_criteria>
  <done>Atomic commit `feat(11.1): migrate solana-sdk 1.18 → 4.x` создан, bisect-safe, готов к Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crates.io → local build | Untrusted third-party code crosses into our binary via cargo |
| `cargo update` resolver → Cargo.lock | Version selection may pull unverified transitive deps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.1-01 | Tampering / Supply-chain | `solana-sdk` 4.x + `solana-client` 4.0.0-beta.7 | mitigate | Verify publisher `anza-xyz` at crates.io; `solana-client 4.0.0-beta.7` user-accepted as beta risk (OQ-1). Downstream Plan 04 runs `cargo audit` to catch any new advisories. |
| T-11.1-02 | Denial of Service | `solana-client` 4.0.0-beta.7 beta quality | accept | Beta version accepted by user decision; no production traffic on this SHA until entire phase (Plans 01-04) completes. Watch loop usage same as 1.18 path (no new API calls added in this plan). |
| T-11.1-03 | Information Disclosure | Borsh deserialization of on-chain bytes in `src/protocols/*.rs` | mitigate | If borsh bump to 1 occurs, existing round-trip tests (`raydium_pool_roundtrip`, etc.) exercise the deserializer on controlled test vectors; any behaviour drift is caught by `cargo test`. |
| T-11.1-04 | Elevation of Privilege | `solana_pubkey::pubkey!` macro accepting literal Drift program ID | accept | Literal is identical (`dRiftyHA39MWEi3m9aunc5MzRF1JYuBsbn6VPcn33UH`); macro migration path does not change the value. [ASSUMED per A2 in research] |
</threat_model>

<verification>
## Exit gate (MANDATORY per D-12)

All three must pass at the final commit's SHA:
```bash
cargo build --all-targets
cargo test --all
cargo clippy --all-targets -- -D warnings
```

Any failure → revert this plan's commit, fix locally, re-commit. Never merge a red SHA.

## Sanity greps (post-commit)
```bash
grep -c 'solana-sdk = "1.18"' Cargo.toml   # expect 0
grep -c 'solana-client = "1.18"' Cargo.toml # expect 0
grep -c 'solana-sdk = "4"' Cargo.toml      # expect 1
grep -rE 'tokio_tungstenite' src/data/cex_ws.rs  # expect 1+ (still there — Plan 02 removes)
grep -c 'edition = "2024"' Cargo.toml       # expect 0 (Plan 03 sets)
```
</verification>

<success_criteria>
- [ ] Cargo.toml пинит `solana-sdk = "4"` и `solana-client = "4.0.0-beta.7"`.
- [ ] Cargo.lock пересобран через `cargo update -p solana-sdk -p solana-client`.
- [ ] Все 5 файлов (`src/main.rs`, `src/rpc.rs`, `src/strategy/risk_monitor.rs`, `src/protocols/orca.rs`, `src/protocols/raydium.rs`) компилируются без ручного переписывания бизнес-логики.
- [ ] `cargo build --all-targets` = 0.
- [ ] `cargo test --all` = 0, не менее 350 тестов зелёные.
- [ ] `cargo clippy --all-targets -- -D warnings` = 0.
- [ ] Atomic commit `feat(11.1): migrate solana-sdk 1.18 → 4.x` создан.
- [ ] Файлы `src/data/cex_ws.rs`, `src/data/ws.rs`, `audit.toml`, `.planning/**` НЕ модифицированы (это scope Plans 02/04/05).
</success_criteria>

<output>
After completion, create `.planning/phases/11.1-migrate-solana-sdk-1-18-to-2-x-and-anchor-0-29-to-0-30-resol/11.1-01-SUMMARY.md`.
</output>
