---
status: partial
phase: 11.1-migrate-solana-sdk-1-18-to-2-x-and-anchor-0-29-to-0-30-resol
source: [11.1-VERIFICATION.md, 11.1-04-SUMMARY.md]
started: 2026-04-17T00:00:00Z
updated: 2026-04-17T00:00:00Z
---

## Current Test

number: 1
name: Manual 5-minute live watch smoke (D-18)
expected: |
  Run `cargo run --release -- watch --pool <POOL> --position-mint <MINT> --cex-symbol SOLUSDT`
  for 5 minutes against live PostgreSQL + internet access to Binance.
  Assert: >= 60 records in pnl_history.price within 5 min, price within Binance SOLUSDT
  mid +/- 1%, no stuck/zero values, <= 5 reconnect warnings.
awaiting: user response

## Tests

### 1. Manual 5-minute live watch smoke (D-18)
expected: |
  Prerequisites: dev PostgreSQL running, DATABASE_URL set, read-only RPC, known pool + position mint.

  ```bash
  export DATABASE_URL="postgres://<dev-user>:<dev-pw>@localhost/tick_liq_dev"
  cargo run --release -- watch \
      --pool <TARGET_POOL_ADDR> \
      --position-mint <POSITION_MINT> \
      --cex-symbol SOLUSDT 2>&1 | tee /tmp/phase11_1_smoke.log &
  WATCH_PID=$!
  sleep 300
  kill -TERM $WATCH_PID
  wait $WATCH_PID 2>/dev/null
  psql "$DATABASE_URL" -c "SELECT price, recorded_at FROM pnl_history WHERE recorded_at > NOW() - INTERVAL '6 minutes' ORDER BY recorded_at DESC LIMIT 20;"
  ```

  Pass criteria:
    - >= 60 records in pnl_history within the 5-minute window (>= 1 write / 5s per D-18).
    - price within Binance SOLUSDT mid +/- 1% at capture time.
    - No stuck price (all 300s single value) and no 0.0 values.
    - grep -c 'cex_ws: connected, subscribed to bookTicker' /tmp/phase11_1_smoke.log >= 1
    - grep -c 'cex_ws: shutdown received' /tmp/phase11_1_smoke.log == 1
    - grep -c 'cex_ws: connect error\|disconnect error' /tmp/phase11_1_smoke.log == 0
    - No reconnect storm (< 5 reconnect warnings in 5 minutes).
result: [pending]

## Summary

total: 1
passed: 0
issues: 0
pending: 1
skipped: 0
blocked: 0

## Gaps

